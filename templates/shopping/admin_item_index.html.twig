<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>管理者ページ</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-giJF6kkoqNQ00vy+HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1" crossorigin="anonymous">
  </head>
  <body>
    <h1 class="my-3 ml-3">商品管理</h1>
    <div class="col-5 ml-3">
      <table  class="table">
        <thead>
          <tr>
            <th>画像</th>
            <th>商品名</th>
            <th>価格</th>
            <th>詳細</th>
            <th>削除</th>
          </tr>
        </thead>
        <tbody>
        {% for value in dataArr %}
          <tr>
            <td><img src="{{constant('shopping\\Bootstrap::ENTRY_URL')}}images/{{value.image}}" alt="{{value.item_name}}" , size: '300x200' class="card-img-top" /></td>
            <td>{{value.item_name}}</td>
            <td>&yen;{{value.price|number_format(0, '.', ',')}}</td>
            <td><a href="{{constant('shopping\\Bootstrap::ADMIN_URL')}}admin_item_show.php?item_id={{value.item_id}}">詳細</a></td>
            <td>削除する</td>
          </tr>
        {% endfor %}
        </tbody>
      </table>
    </div>
    <a href="admin_index.php">ホーム</a>
  </body>
</html>