<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>管理者ページ</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-giJF6kkoqNQ00vy+HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1" crossorigin="anonymous">
  </head>
  <body>
    <h1 class="my-3 ml-3">ユーザー管理</h1>
    <a href="{{constant('shopping\\Bootstrap::ADMIN_URL')}}admin_logout.php" class="btn btn-danger">ログアウト</a>
    <div class="col-5 ml-3">
      <table  class="table">
        <thead>
          <tr>
            <th>ID</th>
            <th>お名前</th>
            <th>メール</th>
            <th>区分</th>
            <th>削除</th>
          </tr>
        </thead>
        <tbody>
        {% for value in users %}
          {% if value.delete_flg == 0 %}
            <tr>
              <td>
                <a href="{{constant('shopping\\Bootstrap::ADMIN_URL')}}admin_user_show.php?user_id={{value.id}}">{{value.id}}</a>
              </td>
              <td>
                {{value.name}}
              </td>
              <td>
                {{value.mail}}
              </td>
              <td>
                {% if value.role == 0 %}一般{% else %}管理者{% endif %}
              </td>
              <td>
                <a href="{{constant('shopping\\Bootstrap::ADMIN_URL')}}index.php?id={{value.id}}" class="btn btn-danger">削除</a>
              </td>
            </tr>
          {% endif %}
        {% endfor %}
        </tbody>
      </table>
    </div>
    <a href="admin_index.php">ホーム</a>
  </body>
</html>